# OGE-366 live regression test (throwaway)

This file exists only to provide a non-empty diff for a throwaway PR that
exercises the `/uat-override` slash-command after the OGE-366 fix landed in
`.github/workflows/uat-override.yml`. The PR is intended to be closed without
merging once the override workflow run is captured as evidence.

Tracker: https://linear.app/ogenticai/issue/OGE-366
Upstream fix: OgenticAI/agent-reviewer#13
